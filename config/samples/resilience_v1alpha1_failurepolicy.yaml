apiVersion: resilience.example.com/v1alpha1
kind: FailurePolicy
metadata:
  name: restart-storm-policy
spec:
  target:
    kind: Deployment
    name: web
  detection:
    windowSeconds: 60
    maxRestarts: 5
  action:
    type: Annotate
    cooldownSeconds: 300

